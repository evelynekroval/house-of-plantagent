# ============================================================================
# RECIPE DATA SOURCE
# ============================================================================
# Where your agent retrieves recipes (API endpoint, file path, or scraping URL)
# Example: "https://api.spoonacular.com/recipes/search" or "./data/recipes.json"
RECIPE_SOURCE_URL=https://api.example.com/recipes

# If using an API that requires authentication
RECIPE_API_KEY=your-api-key-here

# ============================================================================
# LLM / MODEL CONFIGURATION
# ============================================================================
# Model type: "ollama" (local), "groq" (API), "huggingface" (local), etc.
MODEL_TYPE=ollama

# Model name (e.g., "mistral", "llama2", "neural-chat")
MODEL_NAME=mistral

# Model endpoint (local or API)
# For Ollama: http://localhost:11434
# For Groq API: https://api.groq.com
MODEL_ENDPOINT=http://localhost:11434

# API key (if using a paid/free API like Groq)
MODEL_API_KEY=your-api-key-here

# ============================================================================
# NUTRITIONAL GROUNDING
# ============================================================================
# Path to nutritional database (local JSON or SQLite)
# Example: "./data/nutrition_db.json" or "sqlite:///./data/nutrition.db"
NUTRITION_DB_PATH=./data/nutrition_db.json

# Optional: USDA FoodData Central API key for enriching nutrition data
# Get free key at: https://fdc.nal.usda.gov/api-key-signup.html
USDA_API_KEY=your-usda-api-key-here

# ============================================================================
# USER PREFERENCES (OPTIONAL)
# ============================================================================
# Minimum daily nutritional targets for a "well-planned vegan diet"
USER_MIN_PROTEIN_G=50
USER_MIN_IRON_MG=8.0
USER_MIN_B12_MCG=2.4
USER_MIN_CALCIUM_MG=1000

# Disliked ingredients (comma-separated)
# Example: "nuts,soy,gluten"
USER_DISLIKED_INGREDIENTS=

# Preferred cuisines (comma-separated)
# Example: "Indian,Mediterranean,Thai"
USER_PREFERRED_CUISINES=

# ============================================================================
# AGENT LOGGING & DEBUGGING
# ============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Where to save agent logs
LOG_FILE=./logs/agent.log